services:
  tarantool:
    image: tarantool/tarantool:3.2
    container_name: vk-tarantool
    ports:
      - "3301:3301"
    volumes:
      - ./tarantool/init.lua:/opt/tarantool/init.lua:ro
    command: ["tarantool", "/opt/tarantool/init.lua"]
    restart: unless-stopped
